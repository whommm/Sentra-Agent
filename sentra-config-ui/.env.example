CLIENT_PORT=7244

# 后端服务端口（Fastify API / 生产静态服务）
SERVER_PORT=7245

# 允许的跨域来源：
# - 使用 * 表示允许任意来源
CORS_ORIGIN=*

# 日志级别：debug | info | warn | error
LOG_LEVEL=info

# Sentra Agent 根目录（相对于当前目录）
SENTRA_ROOT=..

# 登录安全令牌（密码）
# - 留空：每次启动随机生成一个令牌（推荐开发/临时环境）
# - 设置固定值：使用此固定令牌，方便在生产或稳定环境中部署
# 示例：SECURITY_TOKEN=MY-SECRET-1234
SECURITY_TOKEN=

# 依赖安装镜像配置（Bootstrap/Update 脚本生效）
# 可选方案一：使用 MIRROR_PROFILE 快速切换常用配置
#   - official  或 留空：官方源（npm: registry.npmjs.org；pip: pypi.org）
#   - china / cn / tsinghua：国内加速（npm: registry.npmmirror.com；pip: 清华 Tuna）
MIRROR_PROFILE=cn

# 可选方案二：精细化配置（覆盖 MIRROR_PROFILE 默认值）
# Node 包管理器镜像（npm/pnpm/cnpm 均识别此变量）
# 示例（国内镜像）：NPM_REGISTRY=https://registry.npmmirror.com/
NPM_REGISTRY=https://registry.npmmirror.com

# Python 包索引镜像（pip）
# 主索引（常用：清华 Tuna https://pypi.tuna.tsinghua.edu.cn/simple 或 官方 https://pypi.org/simple）
PIP_INDEX_URL=https://pypi.tuna.tsinghua.edu.cn/simple
# 额外索引（可选，多个时请只填一个，必要时可使用 trusted-host）
PIP_EXTRA_INDEX_URL=
# 受信任主机（逗号分隔），如：pypi.tuna.tsinghua.edu.cn,pypi.org
PIP_TRUSTED_HOSTS=